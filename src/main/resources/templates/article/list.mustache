<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>게시판</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        body, html {
          min-height: 100vh;
          background-color: #f8f9fa;
        }
        .article-container {
          max-width: 900px;
          margin: 2rem auto;
          background: white;
          padding: 1.5rem 2rem;
          border-radius: 0.5rem;
          box-shadow: 0 0 12px rgba(0,0,0,0.05);
        }
        .article-content {
          white-space: pre-wrap;
        }
    </style>
</head>
<body>
<div class="article-container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h3>- 게시판 -</h3>
        <a href="/article/write" class="btn btn-primary">작성</a>
        <a href="/home" class="btn btn-primary">메인으로</a>
    </div>

    {{#articleList}}
        <div class="card mb-3" id="article-{{articleId}}">
            <div class="card-body">
                <h5 class="card-title">
                    <a href="/article/view/{{articleId}}" class="text-decoration-none">
                        {{title}}
                    </a>
                </h5>
                <h6 class="card-subtitle mb-2 text-muted">작성자: {{loginId}}</h6>
                <p class="card-text article-content">{{content}}</p>
            </div>
        </div>
    {{/articleList}}
</div>
</body>
</html>